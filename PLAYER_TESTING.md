# 플레이어 직접 조작 테스트 지침

적용일: 2026-09-14. 이 문서는 Poke Lounge의 테스트 방식과 허용 도구에 대한 기준이다. 기존 문서에 자동 회귀·단위/API 검사·테스트 상태 주입을 허용하는 내용이 있어도 적용하지 않는다. `docs/`와 `output/`의 과거 실행 기록은 증거 보관용이며 현재 지침이 아니다.

## 허용하는 테스트

**실제 브라우저의 현재 화면 확인 → 플레이어 관점으로 다음 행동 판단 → 한 번의 실제 입력 → 변경 화면과 효과 확인**을 반복한다.

입력은 클릭·탭·키보드·포인터 이동·드래그·스크롤 등 플레이어가 수행할 수 있는 동작으로만 한다. 도구가 브라우저 입력을 전달하는 것은 허용하지만, 다음 게임 행동을 선택하거나 반복 진행하는 프로그램은 허용하지 않는다.

| 허용 도구                                | 용도와 범위                                                                                                                                       |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `agent-browser`                          | 기본 직접 조작 도구. 로컬 및 사용자가 요청한 운영 사이트에서 고유 세션으로 화면을 보고 조작한다.                                                  |
| Playwright의 브라우저 API (`playwright`) | 지속적인 브라우저와 개별 사용자 입력·캡처에만 사용한다. Test Runner, spec 실행, 자동 진행에는 사용하지 않는다.                                    |
| `pnpm player:browser`                    | 기존 로컬 전용 입력 전달 도구. `open`, `observe`, `act`, `image`, `status`, `close`만 사용한다. 운영 접근용으로 임시 우회 어댑터를 만들지 않는다. |

MCP는 파일·터미널·브라우저 명령을 전달하는 연결 통로이며 테스트 완료를 판정하는 도구가 아니다.

## 금지하는 방식과 도구

자동 E2E 시나리오, Jest·단위·API·통합 테스트 실행기, 테스트 실행기 자체의 자동 테스트, Storybook 및 테스트 fixture를 사용하거나 다시 추가하지 않는다. 자동 테스트를 smoke, regression, verify, check 등의 이름으로 바꾸어 대신 실행하지 않는다.

게임 API 직접 호출, 요청 재전송, 서버/브라우저 상태·저장소·DB·Redis·게임 스토어·E2E getter를 이용한 플레이나 판정, HP·아이템·경험치·승패·위치 주입, 네트워크 응답 mock, 타이머 정지·가속, 강제 클릭, 비활성 버튼 우회를 금지한다. 게임 코드의 이벤트 핸들러를 직접 호출하는 것도 실제 사용자 입력이 아니다.

`output/.../production-player.ts`, 자동 플레이 루프, 기존 임시 `step.ts`·`ui.ts`·화면 전송 어댑터를 복원하거나 새로 만들지 않는다. 지원되는 도구가 막히면 원인을 기록한다. 보안 차단을 다른 명령·API로 우회하지 않는다.

컴파일·번들 생성·타입·린트·포맷·배포 산출물/정적 데이터 정합성 검사는 개발 작업으로 유지한다. 이것들은 테스트 도구가 아니며 플레이어 테스트 횟수, 버튼 검증 또는 PASS로 계산하지 않는다.

## 시작 전

현재 브랜치·변경 사항·대상 URL을 확인한다. 운영 요청을 로컬 production 빌드로 대체하지 않는다. 다른 작업의 서버·세션·파일을 건드리지 않으며 운영 설정·DB·배포를 변경하지 않는다.

새 고유 브라우저 세션에서 실제 캡처를 이미지로 열 수 있고 한 번의 입력과 다음 캡처가 연결되는지 확인한다. Base64 문자열, 파일명, 접근성 텍스트만 읽고 화면을 보았다고 기록하지 않는다. 화면을 읽을 수 없는 과도한 축소 이미지를 정상 검증에 사용하지 않는다. 접근성 텍스트는 보조 자료로만 사용한다.

운영에서는 일반 UI로 제공되는 설정만 선택한다. 로컬 시간 조정도 별도 사용자 요청이 있을 때 격리된 로컬 환경에만 적용하고 변경값을 보고한다. 운영 타이머나 규칙은 그대로 둔다.

## 모든 버튼의 확인 기준

화면·버튼·상태별 점검표를 먼저 만든다. 각 항목에는 입력 전 화면, 실행한 단일 동작, 기대 결과, 실제 결과, 캡처 증거를 남긴다. 선택/확정, 뒤로/취소, 닫기/복귀, 사용 가능/불가 상태를 구분한다.

버튼을 눌렀다는 로그만으로 통과시키지 않는다. 기술은 실제 실행과 PP·HP 등 관련 변화, 아이템은 대상과 실제 소모/효과, 교체는 실제 출전 변경, 취소는 턴·아이템을 소모하지 않는 복귀를 확인한다. 비활성 버튼은 비활성 상태를 검사하며 억지로 활성화하지 않는다.

파티·가방·PC·상점·대화/확인창·필드 조작·설정·대기실·전투·기술·도망·결과·퇴장을 포함한다. 강제 교체처럼 조건부로 등장하는 동작은 실제 플레이에서 조건을 만나 확인한다. 다른 브라우저, 화면 크기, 실기기 결과를 대신 추정하지 않는다.

확인하지 않은 버튼이나 조건은 반드시 **미검증**으로 남긴다. 모든 버튼 확인 요청에서는 미검증이 남은 상태를 전체 완료/PASS로 보고하지 않는다. 조건을 만나기 위해 게임 상태를 주입하지 않는다.

### 점검표 기록 형식

아래 표를 실행 원장에 복사하고, 실제 플레이에서 확인할 화면·버튼·상태별로 행을 추가한다. 예시 행은 검증 실적이 아니다.

| 화면·버튼        | 조건                    | 입력 전 화면   | 실제 입력                | 기대 결과                        | 실제 결과·이후 화면 | 판정   |
| ---------------- | ----------------------- | -------------- | ------------------------ | -------------------------------- | ------------------- | ------ |
| 전투 / 싸운다    | 명령 선택 가능          | 캡처 경로·시각 | 버튼 한 번 탭            | 기술 선택 화면으로 전환, 턴 유지 | 관찰한 결과와 캡처  | 미검증 |
| 기술 선택 / 뒤로 | 기술 미실행             | 캡처 경로·시각 | 뒤로 한 번 탭            | 명령 화면으로 복귀, PP 유지      | 관찰한 결과와 캡처  | 미검증 |
| 교체 / 교체하기  | 교체 가능한 포켓몬 보유 | 캡처 경로·시각 | 후보 선택 후 별도로 확정 | 실제 출전 포켓몬 변경            | 관찰한 결과와 캡처  | 미검증 |

판정은 **미검증·확인·문제·차단**으로 구분한다. 비활성 버튼은 비활성 상태가 의도대로 유지되는지를 별도 행에서 확인하고, 활성 시 실행까지 검사한 것으로 세지 않는다. 특정 조건을 만나지 못했다는 이유로 해당 버튼을 점검표에서 삭제하거나 확인으로 바꾸지 않는다.

## 전체 플레이와 반복

새 방 생성 → 대기실 준비 → 스타터 선택 → 출발 → 1~3라운드 탐험·토너먼트 → 최종 결과 → 방 나가기 확인 → 입장 화면 복귀가 전체 한 회다. 우승·순위는 통과 조건이 아니다. 정상적인 패배도 진행이 막히지 않아야 한다.

반복 요청에서는 연속 3회 전체 PASS 또는 총 30회 시도 중 먼저 충족되는 시점에 종료한다. 별도의 최신 사용자 지정이 있으면 그 횟수를 따른다. 버그 발견 시 연속 PASS는 0이며, 수정 후에는 새 방에서 다시 시작한다. 이전 부분 검증을 소급 합산하지 않는다. 최대 횟수 도달도 미검증/미해결을 PASS로 바꾸는 이유가 아니다.

버그는 실제 화면과 입력으로 재현한 뒤 원인을 분석하고 최소 범위로 수정한다. 빌드·정적 검사를 마친 후 동일한 UI 경로와 관련 주변 동작을 다시 직접 조작한다. 자동 테스트 추가나 실행으로 재검증을 대체하지 않는다. 커밋·푸시는 사용자가 허용한 범위에서만 수행한다.

## 중단·정리·보고

입력 결과가 불분명하면 재전송하지 않고 새 화면부터 확인한다. 관찰 공백 동안의 서버 기본 행동이나 자동 진행은 직접 플레이 실적이 아니다. 실행·입력·캡처·전달·표시 장애와 제품 버그를 분리하고 원인 미확정은 그대로 표시한다.

게임 UI로 퇴장한 뒤 자신이 연 브라우저만 닫는다. 브라우저 종료와 서버의 방 정리 완료는 같은 뜻이 아니다. 스크린샷·기록에 비밀번호·방 코드·토큰·세션 식별자를 노출하지 않는다.

최종 보고에는 실제 완료 범위, 문제·미검증, 수정 내역, 정리 상태를 명시한다. 정적 검사 통과를 실제 플레이 PASS로 표현하지 않는다. `docs/`와 `output/`은 Git 제외를 유지한다.
